# F2: independent pure-evaluator review

- Read-only Sol/max leaf; no edits, delegation, network/model calls, simulator,
  training, full suite, dependency installation or permission bypass.
- Checkout: `/Users/samueldoane/Documents/ChatGPT/humanoid-harness-astra`.
- Deadline: 20 minutes from launch. Return a verdict before exploring beyond
  this small evaluator. No speculative generic hardening framework.
- Snapshot: `.orchestration/f2-review-20260905T2237.json`. Verify its exact
  root, HEAD, lock and all 13 file hashes before and after review.

## Read

1. Root `AGENTS.md`, `docs/operations/dual-orchestration/README.md`.
2. In that documentation directory: `TASK-F2-t2-evaluator.md`,
   `F2_PARENT_CHECKPOINT.md`, `F2_RESULT.md`, `F1_ACCEPTANCE.md`.
3. New `src/oracle_composition/rewards/target_speed_formula_t2.py` and
   `tests/rewards/test_target_speed_formula_t2.py`.
4. Accepted `rewards/target_speed_formula.py`, `rewards/task_inputs_v2.py`
   under `src/oracle_composition/`, and their scoped tests.

## Verify

- Exact trusted recipe/input classes, forged or missing slots, callback
  ordering, finite numeric conversion and fixed T2 target 3.0.
- Math remains the triangular/affine family with alpha [0.25,4], beta [-10,10],
  output [-10,15]; extreme finite inputs saturate before division.
- No evaluator access to filesystem, dynamic imports/code, simulator state or
  generated callbacks. Dependencies are the accepted recipe/parser and T2
  input modules, not a hidden alternate implementation.
- Runtime ID, old recipe ID and proposed parser ID remain distinct and
  correctly mapped. The parser hash binds the actual reused source.
- Bounds helper returns fresh nested data, with the canonical bytes/hash
  recorded by the builder and independently regenerated by the parent.
- Existing F1/T2 sources/tests/config remain byte-identical; no implicit
  widening of old B0 targets and no registry/model-origin/training admission.
- Tests expose the actual failure boundaries, not only mocks that bypass
  validation. Keep software proof separate from behavior claims.

## Optional bounded check

Use the existing Astra environment, with bytecode/cache writing disabled:

```bash
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q -p no:cacheprovider --assert=plain tests/rewards/test_target_speed_formula_t2.py tests/rewards/test_target_speed_formula.py tests/rewards/test_task_inputs_v2.py
```

These scoped tests need no temporary-file fixtures. If the read-only sandbox
still refuses a write, record that boundary once; do not rerun or bypass it.
The parent already reproduced 73 passes against this snapshot. State which
checks you actually ran, independently from the parent's result.

## Return

- Progress / bottleneck / next step.
- `ACCEPT_T2_EVALUATOR_ONLY` or `REJECT_F2`, with only actionable findings and
  exact source locations; distinguish blockers from optional follow-up.
- Snapshot result before/after, actual test counts and remaining claim limits.
- Peer proposal `20260905T220539.436910Z-95e3e2a4b5954edc81e2bfe32f171cd7`
  has no acceptance yet. Your verdict cannot authorize the shared registry,
  a model call, resources or training.
