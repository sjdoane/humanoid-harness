# Resource slot 01: two-finding closure

- Read-only Sol/max leaf, no nested agents. Only checkout
  `/Users/samueldoane/Documents/ChatGPT/humanoid-harness-astra`, branch
  `astra/reward-loop`. Read AGENTS.md and dual-orchestration README.
- Read `RESOURCE_SLOT_01_REVIEW_RESULT.md` completely: it contains both exact
  findings, prior verdict, negative/positive parent tests and the five repaired
  source hashes. Verify all five at start and end; stop on drift.
- Review those five frozen uncommitted files. Current committed source base
  is `47f38eb`; parent may add docs-only checkpoint/packet commits.
- No edits, imports, tests, temp files, simulator, token acquisition, network,
  API, commits, process control or peer-checkout access. Bounded source reads
  only; no recursive logs or `.orchestration` scans.
- Prior static review passed core exclusion, authority, token identity,
  expiry and complete binding. Review closure of SLOT-01 and SLOT-02 and any
  regression caused by these repairs; do not reopen unrelated research gates.

## Verify

1. Supplied leaf root symlinks fail before helper use and before CLI
   resolve/prepare for reserve/release/board. Real directories still work,
   including ordinary macOS ancestor aliases. Fresh CLI mailbox roots retain
   their existing creation behavior. Target bytes and guard identity are not
   changed by rejected operations.
2. Decoded NUL authority paths and lone surrogates produce typed helper errors
   and bounded CLI JSON, not tracebacks. Cover strings and object keys; inspect
   the new regressions and error translation without importing the module.
3. The repair leaves atomic publication, serialized exact-token release,
   acceptance authority, full binding, expiry and Python 3.9 compatibility
   intact. No new scheduler or runtime authority is introduced.

## Return

- Target 6 minutes; stop analysis and deliver final by 10 minutes. Exact
  watchdog hard deadline is 20 minutes. Do not repeat source/log audits near it.
- Start progress/bottleneck/next-step rows.
- Verdict `ACCEPT_RESOURCE_SLOT_HELPER_ONLY` or `REPAIRS_RESOURCE_SLOT_01_R1`.
- For each finding: closed/open, mechanism evidence, exact file/function/line.
  If open, give one smallest repair and regression. Separate optional remarks.
- Report hashes checked and checks actually performed (static only). Parent's
  37 passes are supplied evidence, not your test execution.
- Fable still owns supervisor integration and terminal cleanup; any heavy run
  requires its own reviewed exact reservation. No training/behavior acceptance.
