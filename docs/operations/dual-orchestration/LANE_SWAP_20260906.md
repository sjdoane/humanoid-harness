# Lane swap: Astra oracle, Fable reward

| status | current truth |
|---|---|
| progress | Fable formally accepted the swap and F3 handoff. FT2R3 finished; Astra owns both combined tracker reviews. |
| bottleneck | Fable's parent validation and final FT2R3 integration commit are pending. No trained result or permission for a cohort. |
| next step | Receive and verify the complete committed chain, integrate in Astra's clean checkout, then dispatch exact-snapshot scientific and robustness reviews. |

## Assignment

| orchestrator | owns | next substantial outcome |
|---|---|---|
| Astra | Reference-oracle generation/composition and tracker integration | Supplied behaviors → state-aware composition → protected failure feedback → revised oracle; distinguish mechanism evidence from tracker effectiveness |
| Fable 5.1 | Task-reward generation and protected feedback/revision | Exact reward hypothesis → validated admission → matched protected evidence → revision, with the oracle frozen |

- User instruction received 2026-09-05 local time; records use 2026-09-06 UTC.
- Both may question and revise their lane strategy against the charter and
  [Lokesh alignment record](FABLE_GOAL_ALIGNMENT.md).
- Shared scientific interfaces still require explicit agreement. No new model,
  paid API, heavy experiment, private publication or push is authorized here.
- Physical checkouts, environments, leases and Fable's main-promotion role stay
  unchanged. `astra/reward-loop` is a historical branch name.

## Transfer protocol

1. Fable's formal acceptance is
   `20260906T040734.663495Z-406cc48ada0f4dfca0a07c315b285b61`, bound to proposal
   `20260906T023515.308859Z-50b08e0c0e3c43739cbd05dca7ab6628`.
   Astra acknowledged reading at 04:10:51 UTC. No further user role approval.
2. FT2R3 has finished. Fable validates and sends its exact committed
   oracle/runtime base, review findings, next packet and dependencies.
   Astra does not read/copy dirty files or duplicate that slice.
3. Astra transfers accepted F3 and its remaining gates below. No further reward
   candidate, revision or builder is dispatched by Astra.
4. Each owner integrates only in its own clean checkout. Inspect the complete
   dependency chain; do not cherry-pick isolated hash-bound runtime pieces.
5. Record acceptance and source identities in the mailbox and this checkpoint.
   Current state: **formal transfer accepted; final source commit pending**.

## Accepted checkpoint: 04:10 UTC

- Exact FT2R3 run: `20260906T022425Z-04c62c36-9f63-4fd1-ada4-124210b16af8`.
  Parent read terminal `SUCCEEDED`, exit 0, 04:06:05 UTC, lease `RELEASED`.
- Worker reports 147 Phase B tests; 402 focused passes/16 skipped; full suite
  1,684 passed/18 skipped/1 deselected/44 baseline failures. These are peer
  results, not Astra reproduction or independent acceptance. No training ran.
- Committed handoff/ADR observed at `b02bad06e357869fe658add844af27ecc389529c`;
  FT2R3 code is not yet included in that commit. Do not import dirty files.
- Astra owns both combined re-reviews; Fable will not dispatch them or another
  oracle/tracker slice. Shared resources and scientific gates are unchanged.
- Fable accepted the COM/root-speed correction, separate T1/T2 study identities,
  and measured tracking-versus-task tradeoffs without relaxing protected gates.
- The provided generic review targets still describe original FT2 and exclude
  earlier findings. New review packets must explicitly close FT1/E003R1/FT2
  findings against the final FT2R1..FT2R3 snapshot, not run those targets verbatim.
- Full-history F3 integration includes the protocol at `7cd98c3` and earlier
  plan/repair receipts, not only the six-file accepted-code commit `dc402e5`.

## Reward handoff to Fable

- Accepted source: `dc402e5d7a42af3bdd1053b349815ed30f34a859`.
  Mailbox handoff: `20260906T024625.662188Z-c1257c8ea0f7498dabad6ea201605258`.
- Independent code verdict: `ACCEPT_F3_STATIC_ONLY`; all four findings closed.
- Parent: 112 focused passes in 0.88 s; 20 pinned files unchanged; Ruff passes.
- One-call protocol has separate conditional approval, but **zero calls** were
  dispatched. Its Astra-specific owner/checkout cannot silently become Fable's.
- Re-pin integrated source and review transfer-specific protocol changes first.
- Runtime certificates, protected evaluation, training and reward improvement
  are still missing. Broader Python reward execution remains separately gated.

## Oracle entry point for Astra

- Last inspected committed peer base: `6d71e8bff03966db0038ebc14a8da59f4bb595f9`.
  This is an audit base, not permission to adopt unfinished work.
- `harness/executor.py` uses whole-controller switching: the old videos and
  Phase A cycles do not demonstrate reference-following composition.
- `phase_b/reference_runtime.py::ComposedReferenceRuntime` emits actual
  reference windows; `phase_b/runtime.py` supplies the 708-D policy path.
- No Phase B training, qualifying tracker trace or causal-use result exists in
  that committed checkpoint. FT2R3 and combined review remain pending there.
- After handoff, prefer a small composition/revision demonstration reusing this
  runtime: distinct supplied behaviors, state-dependent transitions, explicit
  phase/reference identities and retained failure feedback. A deterministic
  mechanism test is not proof that a physical policy follows the references.
- Do not duplicate `phase_b/**`, shared `harness/**`, frozen experiment records,
  or the in-flight worker/import/IPC/RSI repairs. Request the complete handoff.

## Continuity

- Existing task: `humanoid-harness-continuity`; unchanged 30-minute cadence.
- New prompt follows Astra's oracle lane and blocks old reward dispatch.
- The mailbox persists messages; it cannot wake or inject into Claude Code.
- Keep routine checks quiet. Notify on a meaningful result, failure or required
  user decision; never report a pending acknowledgment as a completed switch.

## Reconciliation after Samuel's relayed Fable update

- Read-only runtime check at approximately 03:03 UTC: FT2R3 run
  `20260906T022425Z-04c62c36-9f63-4fd1-ada4-124210b16af8` is live, with recent
  event-file activity and no terminal result. Its own runner logged three
  successful lease renewals. No peer source, hook, lease or process was changed.
- Fable's F3 status is stale: closure is accepted, not pending. Its proposed
  reference change does not invalidate F3's static acceptance, but a materially
  changed experiment must not reuse a mismatched prompt or execution manifest.
- The observed Fable monitor waits for `final.txt` only. Request bounded checks
  of `result.json` and exact process identity too; failure need not produce final
  text. A missing final is not permission to steal a lease or restart a worker.
- Treat fixed-expert T2 as a proposed reward-study variant. Source-library
  root-speed medians are not measured COM task-error headroom. Tracking and
  slowdown objectives may compete; retain the frozen tracking reward and gates.
- A common starting checkpoint is reusable substrate, not automatically a
  common matched control. The current T1 composition oracle differs from a
  fixed-expert T2 oracle. Bind each study's exact oracle, reward, training and
  evaluation identities before claiming a shared baseline or joint attribution.
- The lane swap grants no five-seed cohort authorization. E5 can be deferred
  for an exploratory interface/utility result, not waived for causal-use claims.
