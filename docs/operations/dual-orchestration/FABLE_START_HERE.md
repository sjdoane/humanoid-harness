# Join the second orchestrator

Samuel authorized parallel Fable/Astra research orchestration on 2026-09-04.
Continue your active work, then join at the next clean checkpoint.

1. Native Read:
   `/Users/samueldoane/Documents/ChatGPT/humanoid-harness-astra/docs/operations/dual-orchestration/README.md`.
2. Read the shared inbox at
   `/Users/samueldoane/Documents/ChatGPT/humanoid-harness/.git/harness-coordination/messages/`
   or run the Astra `scripts/research-mailbox inbox fable` under your lease.
3. Reply with kind `acceptance` and `--reply-to` the lane proposal's exact ID,
   or send a `counterproposal` with precise adjustments. Your
   lane is oracle/reference composition, tracker/ABI integration, and causal
   and behavioral tests. Astra's lane is reward generation and the protected
   feedback/revision loop. You each direct Sol/max builders and reviewers.
4. Finish and review B0 without interruption. Send the accepted commit, exact
   contract paths, test receipts, unresolved sandbox canaries, and explicit
   ownership transfer before Astra consumes it. Do not start another reward
   builder after transfer without coordinating.
5. Keep your identity and writer guards. Astra will not edit your checkout.
   Use your lease at a clean checkpoint for mailbox writes; include the
   absolute mailbox directory in its audit scope. A native Read can
   inspect the inbox while a worker owns the writer lease.
6. Poll the inbox at turn start, before new workers/integration, after worker
   results, and about every 30 minutes while active. Send compact evidence and
   blockers. A read acknowledgment alone is not an acceptance.
7. Integrate only reviewed commits at an agreed checkpoint. Each lane owns its
   strategy; shared contracts and heavy compute need explicit peer agreement.
   Keep the two scientific factors separate and the Lokesh goal ledger intact.

Example reply, using your normal Fable lease:

```bash
/Users/samueldoane/Documents/ChatGPT/humanoid-harness-astra/scripts/research-mailbox send --from fable --to astra --kind acceptance --reply-to PROPOSAL_ID --subject "Lane agreement" --body "I accept the proposed paths and ownership. I will finish and review B0, then send its commit and transfer reward ownership. I will poll this mailbox at checkpoints."
```

The mailbox is ready for communication. No claim that this Fable session has
read or accepted it is made until your reply is observed.
